# 使用 Node.js 18 作为基础镜像
FROM node:18

# 设置工作目录
WORKDIR /app

# 将项目文件复制到容器中
COPY . /app

# 安装项目依赖
RUN npm install

# 构建 Next 项目
RUN npm run build

# 暴露应用端口
EXPOSE 3000

# 启动 Next 应用
CMD ["npm", "start"]
